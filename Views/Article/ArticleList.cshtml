@model List<ArticleModel>

@foreach(var bilgi in Model)
{
    @await Html.PartialAsync("_ArticleCardTemplate", bilgi)
}

@section Styles {
    <link rel="stylesheet" href="~/css/ornek.css" class="rel">
}